# Wasserstein Market Regime Clustering

Research and implementation workspace for clustering market regimes using Wasserstein-distance based methods.

## Project Status

Initial repository scaffold.

## Git Workflow

- Work on `main` for now unless a separate feature branch is needed.
- Commit periodically after coherent milestones, tests, or notebook/model changes.
- Do not commit secrets, local virtual environments, caches, or large generated data files.
